<Window x:Class="InfertilityTreatment.Admin.UserManager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InfertilityTreatment.Admin"
        mc:Ignorable="d"
        Title="User Manager"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ========== User Form Section ========== -->
        <GroupBox Header="User Information" FontSize="16" FontWeight="Bold" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <WrapPanel Margin="0,5">
                    <Label Content="Username:" Width="90" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtUserName" Width="180" Margin="5" Padding="4"/>
                    <Label Content="Password:" Width="90" VerticalAlignment="Center"/>
                    <PasswordBox x:Name="txtPassword" Width="180" Margin="5" Padding="4"/>
                    <Label Content="Full Name:" Width="90" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtFullName" Width="180" Margin="5" Padding="4"/>
                </WrapPanel>
                <WrapPanel Margin="0,5">
                    <Label Content="Age:" Width="90" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtAge" Width="100" Margin="5" Padding="4"/>
                    <Label Content="Phone:" Width="90" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtPhoneNumber" Width="150" Margin="5" Padding="4"/>
                    <Label Content="Role ID:" Width="90" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtRoleId" Width="100" Margin="5" Padding="4"/>
                    <Label Content="Is Active:" Width="90" VerticalAlignment="Center"/>
                    <CheckBox x:Name="chkIsActive" Margin="5" VerticalAlignment="Center"/>
                </WrapPanel>
            </StackPanel>
        </GroupBox>

        <!-- ========== User DataGrid ========== -->
        <Border Grid.Row="1" Background="White" CornerRadius="5" BorderBrush="#ccc" BorderThickness="1">
            <DataGrid x:Name="dgUsers"
                      FontSize="14"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      SelectionChanged="dgUsers_SelectionChanged"
                      Margin="5"
                      RowHeaderWidth="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="50"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding UserName}" Width="120"/>
                    <DataGridTextColumn Header="Password" Binding="{Binding Password}" Width="120"/>
                    <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="150"/>
                    <DataGridTextColumn Header="Age" Binding="{Binding Age}" Width="50"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="120"/>
                    <DataGridTextColumn Header="Role ID" Binding="{Binding RoleId}" Width="70"/>
                    <DataGridTextColumn Header="Is Active" Binding="{Binding IsActive}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- ========== Action Buttons ========== -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Update" Width="100" Margin="5" FontSize="14" Background="#28a745" Foreground="White" Click="BtnUpdate_Click"/>
            <Button Content="Delete" Width="100" Margin="5" FontSize="14" Background="#dc3545" Foreground="White" Click="BtnDelete_Click"/>
            <Button Content="Exit" Width="100" Margin="5" FontSize="14" Background="#6c757d" Foreground="White" Click="BtnExit_Click"/>
        </StackPanel>
    </Grid>
</Window>
