<Window x:Class="InfertilityTreatment.Customer.CustomerAppoimentPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InfertilityTreatment.Customer"
        mc:Ignorable="d"
        Title="CustomerAppoimentPage" Height="600" Width="1000">

    <!-- Main Background Image -->
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="D:\PRN212\Image\ViewAppoimentPage.jpg" Stretch="UniformToFill"/>
        </Grid.Background>

        <!-- Main Content Overlay -->
        <Grid Margin="20" Background="#CCFFFFFF">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="📅 View" FontSize="24" FontWeight="Bold" Foreground="#2E6BA0" VerticalAlignment="Center" Grid.Column="0"/>

                <Button Content="⬅ Back to Menu"
                        Width="140"
                        Height="35"
                        Background="#4A90E2"
                        Foreground="White"
                        FontWeight="Bold"
                        Click="BackToMenu_Click"
                        Margin="0,0,5,0"
                        Grid.Column="1"/>
            </Grid>

            <!-- Body -->
            <StackPanel Grid.Row="1">

                <TextBlock Text="Your Appointments" 
           FontSize="18" 
           FontWeight="SemiBold" 
           Margin="0,20,0,10"/>

                <ListView x:Name="AllAppointmentsListView" Height="200">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Date" DisplayMemberBinding="{Binding AppointmentDate, StringFormat='{}{0:MM/dd/yyyy hh:mm tt}'}" Width="150"/>
                            <GridViewColumn Header="Service" DisplayMemberBinding="{Binding Service.ServiceName}" Width="150"/>
                            <GridViewColumn Header="Doctor" DisplayMemberBinding="{Binding Doctor.FullName}" Width="150"/>
                            <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="100"/>

                            <GridViewColumn Header="Action" Width="120">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Cancel"
                                Foreground="White"
                                Padding="5,2"
                                Click="CancelAppointment_Click"
                                IsEnabled="{Binding CanCancel}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="White"/>
                                                    <Setter Property="BorderBrush" Value="Gray"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CanCancel}" Value="True">
                                                            <Setter Property="Background" Value="Red"/>
                                                            <Setter Property="BorderBrush" Value="DarkRed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>


                <!-- Schedule Section -->
                <TextBlock Text="Your Schedule" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

                <!-- Month & Year Filter -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Month:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox x:Name="MonthComboBox" Width="100" SelectionChanged="MonthComboBox_SelectionChanged"/>

                    <TextBlock Text="Year:" VerticalAlignment="Center" Margin="20,0,5,0"/>
                    <ComboBox x:Name="YearComboBox" Width="100" SelectionChanged="YearComboBox_SelectionChanged"/>
                </StackPanel>

                <!-- Schedule List (with background) -->
                <Border CornerRadius="6" Padding="10" Margin="0,0,0,20">
                    <Border.Background>
                        <ImageBrush ImageSource="D:\PRN212\Image\CustomerAppointmentandSchedule.jpg" Stretch="UniformToFill"/>
                    </Border.Background>

                    <TabControl>
                        <TabItem Header="Week 1">
                            <ListView x:Name="Week1ListView">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Day" Width="100" DisplayMemberBinding="{Binding DayName}" />
                                        <GridViewColumn Header="Date" Width="120" DisplayMemberBinding="{Binding Date}" />
                                        <GridViewColumn Header="Time" Width="100" DisplayMemberBinding="{Binding Time}" />
                                        <GridViewColumn Header="Notes" Width="200" DisplayMemberBinding="{Binding Notes}" />
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </TabItem>
                        <TabItem Header="Week 2">
                            <ListView x:Name="Week2ListView">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Day" Width="100" DisplayMemberBinding="{Binding DayName}" />
                                        <GridViewColumn Header="Date" Width="120" DisplayMemberBinding="{Binding Date}" />
                                        <GridViewColumn Header="Time" Width="100" DisplayMemberBinding="{Binding Time}" />
                                        <GridViewColumn Header="Notes" Width="200" DisplayMemberBinding="{Binding Notes}" />
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </TabItem>
                        <TabItem Header="Week 3">
                            <ListView x:Name="Week3ListView">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Day" Width="100" DisplayMemberBinding="{Binding DayName}" />
                                        <GridViewColumn Header="Date" Width="120" DisplayMemberBinding="{Binding Date}" />
                                        <GridViewColumn Header="Time" Width="100" DisplayMemberBinding="{Binding Time}" />
                                        <GridViewColumn Header="Notes" Width="200" DisplayMemberBinding="{Binding Notes}" />
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </TabItem>
                        <TabItem Header="Week 4">
                            <ListView x:Name="Week4ListView">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Day" Width="100" DisplayMemberBinding="{Binding DayName}" />
                                        <GridViewColumn Header="Date" Width="120" DisplayMemberBinding="{Binding Date}" />
                                        <GridViewColumn Header="Time" Width="100" DisplayMemberBinding="{Binding Time}" />
                                        <GridViewColumn Header="Notes" Width="200" DisplayMemberBinding="{Binding Notes}" />
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </TabItem>
                    </TabControl>
                </Border>

                <!-- Rejected Appointments Section -->
                <TextBlock Text="Rejected Appointments" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <Border CornerRadius="6" Padding="10">
                    <Border.Background>
                        <ImageBrush ImageSource="D:\PRN212\Image\Rejected.jpg" Stretch="UniformToFill"/>
                    </Border.Background>

                    <ListView x:Name="RejectedAppointmentListView" Height="150">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Date" DisplayMemberBinding="{Binding Date}" Width="150"/>
                                <GridViewColumn Header="Service" DisplayMemberBinding="{Binding Service}" Width="150"/>
                                <GridViewColumn Header="Doctor" DisplayMemberBinding="{Binding Doctor}" Width="150"/>
                                <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="100"/>
                                <GridViewColumn Header="Reject Reason" DisplayMemberBinding="{Binding RejectReason}" Width="200"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
